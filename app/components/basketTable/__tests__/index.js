const utils = require("../utils");
const mocks = require("./mocks");
const expect = require("chai").expect;

describe("basketTable utils", () => {
    describe("prepareItemsToTotalPriceCalcRequest", () => {
        // ТЕСТЫ
    });
});
