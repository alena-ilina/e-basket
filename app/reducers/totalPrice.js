const initialState = {
    discount: 0,
    price: 0
};

module.exports = function totalPriceReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};
